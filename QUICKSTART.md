# Quick Start Guide - Build Android APK

## Prerequisites Checklist

- [ ] Node.js installed ✓ (you have this)
- [ ] Android Studio installed
- [ ] Environment variables set (ANDROID_HOME, JAVA_HOME)
- [ ] Terminal restarted after setting environment variables

## 5-Step Quick Build

### 1️⃣ Generate Icons
```powershell
# Open generate-icons.html in your browser, download all icons
# Save them to public/icons/ folder
```

### 2️⃣ Sync Assets
```powershell
npx cap sync
```

### 3️⃣ Open Android Studio
```powershell
npx cap open android
```

### 4️⃣ Build APK
In Android Studio:
- **Build → Build Bundle(s) / APK(s) → Build APK(s)**

OR from command line:
```powershell
npm run cap:build:apk
```

### 5️⃣ Install on Phone

**Option A - USB Cable:**
```powershell
# Enable USB debugging on your phone first!
adb install android\app\build\outputs\apk\debug\app-debug.apk
```

**Option B - Manual Transfer:**
- Copy `android\app\build\outputs\apk\debug\app-debug.apk` to your phone
- Open the APK file on your phone
- Install it

## Running the App

1. Start your backend server:
```powershell
npm start
```

2. Find your computer's IP address:
```powershell
ipconfig
# Look for IPv4 Address (e.g., 192.168.1.100)
```

3. In the app, tap Settings ⚙ and enter:
```
http://YOUR_IP_ADDRESS:3000
```

4. Save and start chatting!

---

## If You Get Stuck

1. **Can't build?** → Read `BUILD-ANDROID-APK.md` for detailed troubleshooting
2. **No Android Studio?** → Download from https://developer.android.com/studio
3. **Environment variables?** → See full guide in `BUILD-ANDROID-APK.md`
4. **App won't connect?** → Make sure both phone and PC are on same WiFi

---

## File Structure

```
ai-agent-system/
├── public/              # Your web app (will be converted to APK)
│   ├── index.html      # Main app file (responsive!)
│   ├── manifest.json   # PWA manifest
│   └── icons/          # App icons (generate these first!)
├── android/            # Generated by Capacitor
│   └── app/build/outputs/apk/debug/
│       └── app-debug.apk  # Your installable APK! 📱
├── capacitor.config.ts # Capacitor configuration
├── generate-icons.html # Icon generator (open in browser)
└── BUILD-ANDROID-APK.md # Full detailed guide
```

---

**For the complete guide with troubleshooting, see:** `BUILD-ANDROID-APK.md`
